---
title: F-codes
description: >-
  The VP415 F-code command set, plus the `?D` / `?P` / `?U` / `?=` responses read back from a real player.
---

# F-codes

The VP415 F-code command set, plus the `?D` / `?P` / `?U` / `?=` responses read back from a real player.

!!! note "Not written yet - this page lands in phase 6"

    The content will be built from:

    - `vp415Fcode.xlsx` as a searchable table
    - Disc information bytes per Domesday disc side
